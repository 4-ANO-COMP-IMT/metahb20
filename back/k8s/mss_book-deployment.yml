apiVersion: apps/v1
kind: Deployment
metadata:
  name: mss-book-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mss-book
  template:
    metadata:
      labels:
        app: mss-book
    spec:
      containers:
      - name: mss-book
        image: raphaelray/back-mss_book:latest 
        ports:
        - containerPort: 4001
        env:
        - name: PORT_MSSBOOK
          value: "4001"
        - name: EVENTBUS_URL
          value: "http://eventbuss:4002/event"
--- 
apiVersion: v1
kind: Service
metadata:
  name: mss-book-service
spec:
  type: NodePort  
  selector:
    app: mss-book
  ports:
    - protocol: TCP
      port: 4001
      targetPort: 4001
      nodePort: 30001  